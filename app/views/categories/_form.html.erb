<%= form_with(model: category, local: true) do |form| %>
	<%= render 'layouts/errors', item: category%>
<p>
  <%= form.text_field :name, placeholder: "Name"%>
</p>
<p>
  <%= form.text_area :description, placeholder: "Description"%>
</p>
<div class="submit">
  <%= form.submit %>
</div>
<% end %>