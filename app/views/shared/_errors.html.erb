<% if item.errors.any? %>
<div class="errors">
  <h3><%= pluralize(item.errors.count, "error") %> prohibited this <%=item.name%> from being saved:</h3>
  <ul>
    <% item.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>